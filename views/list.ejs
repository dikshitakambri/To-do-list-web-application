<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="CSS/styles.css">
        <title>To-do List</title>
    </head>
    <body>
        <div class="box">
            <h1 id="heading" style="text-align: center;"><%= kindofDay %></h1>
        </div>
        <div class="box">
            <% for(var i=0; i < newItems.length; i++) { %>
                <div class="item">
                    <input type="checkbox">
                    <p><%= newItems[i] %></p>
                </div>
            <% } %>
            <form action="/" method="POST" class="item">
                <input type="text" name="item" placeholder="New Item" autocomplete="off">
                <button>+</button>
            </form>
        </div>
        
    </body>
</html>